import styled from "styled-components";
import { setRem, setColor, setBorder, setBackground, setTransition } from "../../styles";

export const PrimaryBtn = styled.button`
padding: ${setRem(8)};
font-size: ${setRem()};
background: ${setColor.lightAccent};



`;